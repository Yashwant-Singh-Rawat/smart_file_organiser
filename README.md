# smart_file_organiser
Smart Python-based file automation utility that organizes files by type, detects duplicates using hashing, handles filename conflicts, and generates execution logs using only Python standard libraries.
